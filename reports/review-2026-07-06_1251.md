All claims verified. The audit is complete — here is the full report.

# Setup Audit Report - 2026-07-06

## Summary
- Lint issues confirmed/fixed: 0/0
- New findings: 16 (critical: 0, high: 3)
- Safe fixes applied: 5 (24 files touched)
- Recommendations requiring user decision: 11

## Findings

### agents: Every agent's "Tool Awareness" section instructs tools its whitelist cannot reach — probe-proven unexecutable
- Evidence: A live capability probe (technical-writer, whose own body at `technical-writer.md:274` says to use ToolSearch) confirmed whitelisted subagents get **no ToolSearch and no MCP tools** — it saw only its 8 whitelisted tools. Sweeps of all 30 agent files found none fully clean. Worst cases: `debugger.md:32-33` makes SigNoz MCP `get_trace`/`search_logs` a *mandatory first step* for production incidents (whitelist: Read/Write/Edit/Bash/Glob/Grep — no MCP); `backend-architect.md:56`, `system-architect.md:55`, `prompt-engineer.md:57` instruct Agent-tool delegation without the Agent tool; `multi-agent-coordinator.md:270-271`, `task-distributor.md:268`, `workflow-orchestrator.md:270-272`, `data-engineer.md:271-274` cite TaskCreate-family/CronCreate tools not in any whitelist; `business-panel-experts.md:252` instructs WebFetch/WebSearch it lacks. The sections were bulk-appended (4 files even lack the blank line before the heading, e.g. `debugger.md:291`).
- Action: recommendation — this is 30 deliberately-authored files from the Jul 5-6 overhaul, so pick a direction (Rec 1) rather than my piecemeal edits.

### settings: `CLAUDE_CODE_MAX_OUTPUT_TOKENS=8000` truncates long subagent outputs — killed 2 of 3 audit sweeps today
- Evidence: `settings.local.json:21`; both agent-file sweep subagents died with "response exceeded the 8000 output token maximum" and had to be resumed for compact re-reports.
- Action: recommendation (Rec 2).

### skills/commands: `commands/code-review.md` shares its name with the built-in `/code-review` that ultrareview depends on
- Evidence: `commands/code-review.md` registers as skill `code-review`; the harness's ultrareview path is invoked as `/code-review ultra`. If the user command shadows the built-in, the cloud review entry point silently degrades. Same pattern: gstack's `review` skill vs the built-in `review` (both visible in the catalog).
- Action: recommendation (Rec 3) — resolution order isn't observable from a non-interactive session.

### rules: 28 dead "See skill:" references in ECC language rules pointed at upstream skills never installed here
- Evidence: `rules/ecc/{python,golang,rust,java,php}/*.md` referenced `python-patterns`, `laravel-tdd`, `jpa-patterns`, `rust-patterns`, etc. — none exist in this setup (grep of the skill catalog).
- Action: **fixed** — removed all 28 across 20 files, dropped now-empty "## Reference" headers, kept the 2 valid `security-review` refs (`java/security.md:99`, `rust/security.md:140`). Verified by re-grep: only the valid two remain.

### skills: playwright skill's wrapper path pointed at a Codex install that doesn't exist
- Evidence: `skills/playwright/SKILL.md:38-42` set `PWCLI="$CODEX_HOME/skills/playwright/..."` (default `~/.codex`, which doesn't exist); the script actually lives at `~/.claude/skills/playwright/scripts/playwright_cli.sh`.
- Action: **fixed** — rewired to the real path.

### commands: `refactor-code.md` had no frontmatter at all (only file in the corpus without it)
- Evidence: file began at the H1; registration was working only by filename/heading convention.
- Action: **fixed** — added `allowed-tools`/`argument-hint`/`description` frontmatter matching its siblings.

### commands: `init/fullstack.md` delegated to a command format that doesn't resolve
- Evidence: lines 19-20 said `/init-{framework}`; actual commands are `init:fastapi`, `init:nextjs`, etc.
- Action: **fixed** — corrected to the `init:{framework}` form with real examples.

### rules: RULES.md referenced the "Task tool," the retired name for the Agent tool
- Evidence: `RULES.md:35`; ambiguous now that TaskCreate/TaskUpdate are distinct harness tools.
- Action: **fixed** — now reads "Use the Agent tool (subagent searches) when scope uncertain."

### agents: ~250 lines of dead SuperClaude persona scaffolding in two opus agents
- Evidence: `socratic-mentor.md:153-287` ("Integration with SuperClaude Framework": persona_triggers, confidence thresholds, handoffs to nonexistent "analyzer/scribe/qa/refactorer" personas — ~46% of the file); `business-panel-experts.md` carries 9 embedded persona YAML blocks (lines 16-229). No persona/auto-activation mechanism exists in this harness; it's inert token payload on every invocation.
- Action: recommendation (Rec 4) — too large to count as a mechanical trim.

### plugins/skills: beads plugin docs are ahead of the installed bd CLI
- Evidence: plugin skills say "sync (deprecated — use bd dolt push/pull)" and "import (removed)", but installed `bd 0.47.1` has **no `dolt` subcommand** while `bd sync` works normally. AGENT_ROUTER's `bd sync` guidance is correct for this machine.
- Action: recommendation (Rec 7); no config change made — the router text was verified, not fixed.

### skills: `connect-chrome/` and `open-gstack-browser/` are byte-identical duplicate gstack dirs
- Evidence: both SKILL.md files carry `name: open-gstack-browser`, v0.2.0, identical description; both dirs gstack-managed (0700, stamped Jul 6 11:09); only `connect-chrome` surfaces in the catalog.
- Action: recommendation (Rec 5) — gstack-managed, so per the brief I audited presence and did not touch internals.

### settings/MCP: registration sprawl — playwright ×3, context7 ×2, one dead server, default DB creds
- Evidence: playwright registered in `settings.json:60`, `~/.claude.json` global, and the `/home/geoff` project entry; context7 in global config and `~/.mcp.json`; `sentry` in `~/.mcp.json` never appears among connecting/available servers; `postgresql` uses literal `postgres:postgres@localhost` (a live local DB — port 5432 is listening — so functional, but it's the pattern your own security rules flag); `settings.local.json:2` sets `enableAllProjectMcpServers: true`, auto-trusting any project's `.mcp.json`.
- Action: recommendation (Rec 6).

### agents: least-privilege — two grants exceed the role
- Evidence: `business-panel-experts` holds Write but produces conversational analysis only (sweep found no file-authoring duty); `multi-agent-coordinator` holds Write+Edit while its body says specialists do the code work (borderline).
- Action: recommendation (Rec 9). Reverse direction (tools missing for the job) is covered by the systemic finding above.

### commands: the two ralph launchers are near-duplicate symlinks with one stale example
- Evidence: `ideate-and-build.md` and `specs-to-ralph.md` are symlinks into `~/development-projects/beads-in-ralph/`; bodies share near word-for-word TDD/ralph-launch/beads boilerplate; `specs-to-ralph.md:17` shows a stale `/project:specs-to-ralph` invocation.
- Action: recommendation (Rec 8) — they're another project's files; editing through the symlink would change that repo silently.

### commands: `sc/git.md` documents an argument contract it never wires up
- Evidence: its Usage block defines `[operation] [args] [--smart-commit] [--branch-strategy]` but the body never consumes `$ARGUMENTS` and contains no concrete commands — unique among all commands audited.
- Action: recommendation (Rec 11).

### MCP: linear-server requires OAuth and is unusable until authorized
- Evidence: harness reports it needs authentication; it's configured for the `/home/geoff` and `dm-support-beads` projects. This session is non-interactive so I can't run the flow.
- Action: recommendation (Rec 10) — authorize via `/mcp` in an interactive session, or remove it from those project configs if unused.

## Hunt Log
- **Agents**: fanned out 2 subagents over all 30 files (tool-vs-body, dead refs, structure); independently grepped every `model:` pin — all 30 match the AGENT_ROUTER roster, exactly 10 fable pins, none downgraded; ran a live probe proving whitelisted subagents get no ToolSearch/MCP; personally re-verified every cited line before reporting.
- **Plugins**: cross-checked `installed_plugins.json` ↔ `enabledPlugins` ↔ 4 marketplace clones (no dead entries, all marketplaces backing ≥1 plugin); claude-mem 9.0.17 = marketplace latest (upstream idle since Feb); warp plugin justified — `warp-terminal` is installed at `/usr/bin/warp-terminal`; read claude-mem's hook manifest (2 node procs per tool call, 3 on Stop — unchanged, baseline now recorded in memory); blocklist contains only registry test entries.
- **Skills/commands**: subagent swept 6 non-gstack skills + 13 commands + `init/`×6 + `sc/git.md`; resolved all symlinks (none broken); gstack presence confirmed as a git clone at v1.58.5.0 (2026-06-25); mapped duplicate clusters (code-review family, review collision, security trio, ralph twins, browser-skill twins).
- **Settings/hooks**: read both settings files; all 4 hook scripts py_compile clean and use `python3` + `$HOME`-absolute paths (resolve from any cwd); GateGuard verified live — it gated my `rm -rf` and I satisfied the fact gate; diffed against the Jun-4 backup: posture *improved* (`skipDangerousModePermissionPrompt` true→false; gateguard/config-protection hooks and model:fable added). Standing flags: `remoteControlAtStartup: true`, `enableAllProjectMcpServers: true`.
- **Rules layer**: verified CLAUDE.md `@`-includes, INDEX.md lazy-load map vs disk (9 language dirs present; 53 rule files + INDEX = 54 found, matching ECC-ADDITIONS' count), ECC-ADDITIONS claims vs reality (contexts/dev|review|research.md exist; hook modes match the code), performance.md's "10 fable agents + main loop" claim (true: settings.json `model: fable`), and `bd sync` against the installed CLI (works; no deprecation).
- **Loops**: ralph caps present and enforced (`ralph_loop.sh:22-23`, `:1086-1094`); both launchers disable claude-mem *and* warp in workdirs (`specs-to-ralph.md:216-222`, `ideate-and-build.md:176-182`); AGENT_ROUTER loop rules remain notification-first with polling reserved for external state.
- **Upstream drift**: shallow-cloned davila7/claude-code-templates (left at `/tmp/cct-review`); 17/30 roster agents exist upstream; no materially improved upstream versions — local copies are equal or richer, and the two much-larger upstream files (prompt-engineer 142 vs 57, security-engineer 275 vs 60) are generic bulk your slimmed rewrites deliberately dropped.

## Recommendations
1. **Pick one resolution for the systemic Tool Awareness gap** (highest value): either (a) add the named MCP/ToolSearch/Task-tool capabilities to each agent's `tools:` whitelist where they fit the role, or (b) trim the bullets down to whitelist-only capabilities — consistent with your slimming direction. Prioritize `debugger.md` (unavailable tools are *mandatory* steps) and the three Agent-tool delegation bullets (backend-architect, system-architect, prompt-engineer). I can execute either direction across all 30 files on request.
2. Raise `CLAUDE_CODE_MAX_OUTPUT_TOKENS` in `settings.local.json` (e.g. 16000-32000) or unset it — at 8000 it demonstrably truncates subagent final reports.
3. In an interactive session, run `/code-review ultra` once to confirm the built-in still resolves; if your `commands/code-review.md` shadows it, rename yours (e.g. `quality-review`). Same check for `/review` vs gstack's `review`.
4. Strip the SuperClaude persona blocks from `socratic-mentor.md` (lines 153-287) and `business-panel-experts.md` (9 YAML blocks) — roughly 250 lines of inert payload on two opus agents.
5. Remove the stale gstack browser dir (`open-gstack-browser/` vs `connect-chrome/` — identical content); `/gstack-upgrade` may reconcile it, otherwise report upstream.
6. Consolidate MCP config: keep one playwright registration (drop `settings.json:59-66` and the project-level dupe), drop the context7 duplicate from `~/.mcp.json`, remove or authorize `sentry`, and consider setting `enableAllProjectMcpServers` to false in favor of explicit `enabledMcpjsonServers`.
7. Upgrade the `bd` CLI when convenient (plugin docs already assume the dolt-backed version) — until then ignore the plugin's `bd dolt` guidance; AGENT_ROUTER needs no change.
8. In the `beads-in-ralph` repo (source of the symlinked commands): fix the stale `/project:specs-to-ralph` example and consider merging `ideate-and-build`/`specs-to-ralph` into one command with a "have a spec already?" branch.
9. Drop `Write` from `business-panel-experts`; consider dropping `Write, Edit` from `multi-agent-coordinator` if you want it to stay a pure orchestrator.
10. Authorize `linear-server` via `/mcp` in an interactive session (claude.ai connectors: via connector settings), or remove it from the `/home/geoff` and `dm-support-beads` project configs if unused.
11. Either wire `$ARGUMENTS` into `sc/git.md` or retire it — it's the last SuperClaude command and currently documents arguments it never reads.

Fixes applied this run: ECC dead-reference cleanup (20 files), `playwright/SKILL.md` path, `init/fullstack.md` command refs, `refactor-code.md` frontmatter, `RULES.md` tool naming — each verified by re-read or re-grep. An audit baseline (versions, hook costs, open items) is saved to memory for next week's drift comparison. The upstream clone remains at `/tmp/cct-review` if you want to inspect it.
