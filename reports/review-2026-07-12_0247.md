# Setup Audit Report - 2026-07-12

## Summary
- Lint issues confirmed/fixed: 0/0 (re-ran the lint after my edits: still 0)
- New findings: 16 (critical: 1)
- Safe fixes applied: 6 (10 edits across 7 files, each verified by re-read + re-lint)
- Recommendations requiring user decision: 9

## Findings

### settings: `skipDangerousModePermissionPrompt` is `true`, contradicting the recorded 2026-07-06 decision to set it `false`
- Evidence: settings.json:53; the Jun-4 backup (`settings.json.backup.1780624061`) also shows `true`, and settings.json's mtime is Jul 6 15:14 — two hours *after* the overhaul session applied its recommendations. Either the false-set never persisted or it was reverted that afternoon.
- Action: recommendation — this is the audit's only critical-severity item since the weekly cron itself runs `claude --dangerously-skip-permissions`. I corrected the overhaul memory so it no longer claims false is in effect; flipping the flag is your call.

### skills: claude-api skill mandated a superseded model (`claude-opus-4-6`) from a 2026-02-17 snapshot
- Evidence: skills/claude-api/SKILL.md:15,141 ("ALWAYS use claude-opus-4-6… non-negotiable") and shared/models.md listing Opus 4.6/Sonnet 4.6 as current — every API app built via this skill would pin a legacy model.
- Action: fixed — fetched the live models overview (per the skill's own live-sources.md refresh path) and updated the two model-selection authorities: default is now `claude-opus-4-8` with `claude-fable-5` for highest capability, current table (Fable 5 / Opus 4.8 / Sonnet 5 / Haiku 4.5 with real pricing incl. Sonnet 5 intro pricing), and models.md fully rebuilt (Opus 4.1 marked deprecated, retires Aug 5 2026; Mythos noted as invitation-only). The ~100 `claude-opus-4-6` occurrences in code examples were left alone — that's still a valid legacy model, and hand-editing a generated snapshot's API-behavior notes I can't verify would be riskier than the staleness.

### rules: AGENT_ROUTER.md told the coordinator to surface MCP capabilities in specialist prompts — specialists have no MCP access, and it named a Sentry server that doesn't exist
- Evidence: AGENT_ROUTER.md:98; probe-confirmed twice this session (spawned code-reviewer and socratic-mentor: no mcp__* tools, no ToolSearch). No sentry server in ~/.claude.json or ~/.mcp.json (signoz only).
- Action: fixed — rewrote item 5 to state the reality (no MCP for whitelisted agents; give Bash/skill equivalents; MCP reachable only from the main session or `tools: *` agents) and dropped Sentry.

### settings/MCP: github MCP server is allowlisted but configured with a literal `<YOUR_TOKEN>` placeholder
- Evidence: ~/.mcp.json `"GITHUB_PERSONAL_ACCESS_TOKEN": "<YOUR_TOKEN>"`; it's in settings.local.json's `enabledMcpjsonServers`, connects every session, and offers 26 tools that can only return 401s.
- Action: recommendation — either supply a real token or remove `github` from the allowlist (gh CLI already covers GitHub). Last week deferred this; it's still burning a per-session npx spawn for zero capability.

### scripts: update-agents.sh is a live clobber hazard with a false header
- Evidence: scripts/update-agents.sh:3 claims "Run via cron: 0 3 * * 0" but crontab has no such entry; the script pulls 16 agents wholesale from davila7/claude-code-templates — running it manually would overwrite the fable pins and all 2026-07-06 body customizations.
- Action: recommendation — archive it to backups/. I did not delete it per audit rules.

### agents: security-auditor's body and referenced skills require Bash it doesn't have
- Evidence: agents/security-auditor.md:38,152,160 ("Network scanning", "Configure tools", "Execute testing") and lines 274-275 telling it to invoke `security-audit`/`dependency-audit` — both skills run scanners via Bash; its whitelist is Read/Grep/Glob/Skill.
- Action: recommendation — either grant Bash (privilege expansion, your call) or trim the body to read-only audit scope.

### agents: ~1,100 lines of VoltAgent template mass survives in 5 files, including fabricated-metrics "delivery notification" templates
- Evidence: security-auditor (275 lines), task-distributor (269), technical-writer (274), workflow-orchestrator (271) carry topic checklists, status-JSON blocks implying a nonexistent agent status bus, and canned quotes with invented numbers ("Reviewed 347 controls… Compliance score: 87%" at security-auditor.md:208); multi-agent-coordinator.md:33-81 describes a fictional inter-agent message bus (WebSocket/GraphQL subscriptions). Status-JSON blocks appear in 9 files total.
- Action: recommendation — these need one coherent trim pass per file, not unattended nibbles at 3am. The fabricated-metrics blocks are the worst offenders (they train agents to invent numbers).

### agents: task-distributor held Write/Edit it never uses
- Evidence: full-body sweep found no concrete file-writing instruction; its Tool Awareness (lines 265-269) describes Agent + Bash/bd + Read as the actual mechanism.
- Action: fixed — whitelist is now `Read, Bash, Glob, Grep, Agent` (matches the coordinator's least-privilege shape).

### agents: deep-research-agent instructed cross-session memory that doesn't exist
- Evidence: former lines 124-128 ("Check for similar past research… Store valuable findings… Build knowledge over time") — a Memory-MCP-flavored leftover; subagents have no persistence.
- Action: fixed — removed the dead block (within-run pattern-recognition advice kept). Also fixed pm-agent.md:97, whose "Will Not" parenthetical claimed it "delegates to specialist agents" (it has no Agent tool; the parent does the launching).

### rules: TodoWrite referenced in RULES.md and AGENT_ROUTER.md — that tool no longer exists in the current harness
- Evidence: RULES.md:8, AGENT_ROUTER.md:106,110; both probes confirmed no TodoWrite; the harness now uses TaskCreate/TaskUpdate.
- Action: fixed — renamed to "Task list (TaskCreate…)" / "in-session task list" in all three spots. Left ecc/common/hooks.md's "TodoWrite Best Practices" section alone (upstream-sourced file; see recommendation 9).

### skills: code-review-checklist duplicates two other review surfaces
- Evidence: skills/code-review-checklist/SKILL.md is a generic review checklist; ecc/common/code-review.md (autoloaded every session) and the built-in /code-review skill already cover it; gstack /review covers branch review.
- Action: recommendation — archive the skill; the autoloaded ECC rule makes it redundant.

### MCP: linear-server still un-OAuthed; Gmail connector needs re-auth
- Evidence: linear-server is defined in ~/.claude.json but was absent from this session's connected servers (needs interactive /mcp OAuth — open since last week); the harness flagged claude.ai Gmail as requiring authorization this session.
- Action: recommendation — authorize Gmail via claude.ai connector settings and run /mcp for Linear in an interactive session (or remove the entry).

### housekeeping: three stale artifacts at ~/.claude root
- Evidence: templates/ (Aug 2025, referenced by nothing — pre-overhaul spec templates whose commands were archived), .superclaude-metadata.json (SuperClaude retired 2026-07-06), settings.json.backup.1780624061 (Jun 4).
- Action: recommendation — move all three to backups/cleanup. Note: contexts/ looked dead but is documented as deliberate on-demand posture files in ECC-ADDITIONS.md §3 — left alone.

### agents: socratic-mentor still carries SuperClaude persona-format headers
- Evidence: socratic-mentor.md:11-13 ("**Identity**", "**Priority Hierarchy**: Discovery learning > …") — last week's baseline claimed persona YAML was stripped; the format survives here (the book-knowledge YAML blocks are legitimate teaching content, though).
- Action: recommendation — cosmetic reformat only; the agent functions.

### rules: ecc/common/hooks.md contradicts actual practice
- Evidence: it says "Never use dangerously-skip-permissions flag" while agent-review.sh (this cron) uses exactly that flag headlessly, and its TodoWrite section names the retired tool.
- Action: recommendation — reword the ECC rule (e.g. "except for unattended cron audits") or accept the documented exception.

### plugins: claude-mem SessionStart injection grew 15.4KB → 17.9KB week-over-week (+16%)
- Evidence: this session's SessionStart hook persisted-output (17.9KB, 50 observations); baseline recorded 15.4KB.
- Action: none this week — noted in the new baseline; worth a trim (observation cap) if it keeps growing. Also newly recorded: claude-mem fires 2 node procs per user prompt (UserPromptSubmit) and warp adds 1 bash per tool call + per prompt — unchanged since install, just previously unmeasured.

## Hunt Log
- Agents: extracted all 30 frontmatters (models/tools) and diffed against the AGENT_ROUTER roster — 10 fable/7 opus/11 sonnet/2 haiku, zero drift, zero downgrades; two parallel subagents read all 30 bodies for exec-vs-whitelist, dead refs, description bloat, and leftover template mass; two live capability probes (code-reviewer, socratic-mentor) re-verified the no-MCP/no-ToolSearch fact and exposed that single probes can under-report their own tool lists.
- Plugins: compared installed_plugins.json against all 4 marketplace manifests (all versioned plugins current; superpowers sha-pin matches; frontend-design auto-updated today); enumerated every plugin hooks.json and counted per-event process cost.
- Skills/commands: verified gstack presence (v1.58.5.0 clone) without auditing internals; confirmed commands/sc holds only git.md and init/ matches the 6 init:* skills; hunted duplicate clusters across the 5 standalone skills; grepped the claude-api skill for stale model IDs (bounded the fix to its 2 selection authorities).
- Settings/hooks: py-compiled all 4 custom hook scripts; confirmed every hook command (settings + plugins) uses $HOME/${CLAUDE_PLUGIN_ROOT} absolute paths so they resolve in any project dir; checked binary deps (jq/node/gdbus/claude/bd/ralph present; notify-send absent but gdbus fallback is coded); diffed settings.json against the Jun-4 backup.
- Rules: grepped CLAUDE.md/AGENT_ROUTER/RULES/rules/ecc for Sentry, sc:*, MCP, TodoWrite, and contexts/templates references; verified ecc INDEX language dirs exist and performance.md's fable claims match reality.
- Loops: ralph caps intact at ralph_loop.sh:22-23; both ralph launchers still write claude-mem+warp disables into workdir settings; AGENT_ROUTER loop rules remain notification-first (no polling of harness-tracked agents); crontab holds only the 3 known jobs.
- Upstream drift: shallow-cloned davila7/claude-code-templates and compared the 16 mapped agents — no material improvements to import (upstream uses multi-example description blobs you've rejected, and its task-distributor lacks the Agent tool entirely); /tmp/cct-review cleanup is handled by the audit script's exit trap.
- MCP: mapped all 19 connecting servers to their definitions (9 user-scope, 6 allowlisted project-scope, grep built-in, 4 claude.ai connectors), which surfaced the github placeholder token and linear's missing OAuth.

## Recommendations
1. Decide `skipDangerousModePermissionPrompt`: keep `true` (convenience for this cron) or restore the recorded intent of `false` — settings.json:53.
2. Fix or drop the github MCP server: put a real PAT in ~/.mcp.json or remove `github` from `enabledMcpjsonServers` in settings.local.json (gh CLI covers it).
3. Authorize the claude.ai Gmail connector (claude.ai settings) and OAuth linear-server via /mcp in an interactive session, or delete the linear entry.
4. Archive scripts/update-agents.sh to backups/ — it would clobber your customized agents and its cron claim is false.
5. Pick a lane for security-auditor: grant Bash (so its scanners and the security-audit/dependency-audit skills work) or strip the execution language from its body.
6. Schedule one deliberate trim pass over security-auditor, task-distributor, technical-writer, workflow-orchestrator, and multi-agent-coordinator (~1.1k lines of template mass, fabricated-metrics quotes, fictional message-bus/status-bus content).
7. Archive the code-review-checklist skill (redundant with the autoloaded ecc/common/code-review.md and built-in /code-review).
8. Tidy ~/.claude root: move templates/, .superclaude-metadata.json, and settings.json.backup.1780624061 into backups/.
9. Low priority: re-vendor the claude-api skill from its source to refresh the ~100 legacy model IDs in code examples (I fixed only the selection authorities); reformat socratic-mentor's persona headers; update ecc/common/hooks.md (TodoWrite naming + the skip-permissions rule the cron itself violates).
