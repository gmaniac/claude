Continuing the report:

### MCP Coverage (cont.)
- **GitHub** → also now requirements-analyst ✓
- **PostgreSQL** → data-engineer, backend-developer, fullstack-developer, performance-engineer, security-auditor/engineer, debugger, root-cause-analyst ✓
- **SigNoz / Sentry** (observability) → debugger, root-cause-analyst, performance-engineer, security-engineer, devops-engineer/architect, multi-agent-coordinator inventory ✓ **(fixed this cycle)**
- **shadcn / Magic** → frontend-architect/developer, fullstack-developer ✓
- **Memory** → pm-agent, system-architect, socratic-mentor, learning-guide, requirements-analyst ✓
- **Sequential-thinking** → deep-research, system-architect, socratic-mentor, business-panel-experts ✓
- **Tavily / Fetch** → deep-research-agent, requirements-analyst, business-panel-experts ✓
- **morphllm-fast-apply** → refactoring-expert ✓
- **grep** (`mcp__grep__searchGitHub`) → deep-research, system-architect, prompt-engineer, learning-guide ✓

**Gaps where an MCP exists but no agent references it:**
- **filesystem** — intentionally unreferenced; agents use native Read/Write/Edit/Glob/Grep, which is correct (the filesystem MCP is redundant for in-tree work).
- **claude.ai Gmail / Google Calendar / Google Drive / Instacart** — personal-productivity MCPs with no SWE-agent fit. No action; not a roster gap.
- **chrome-devtools** vs **playwright** — both browser MCPs are configured and both are referenced; some overlap but each has distinct strengths (DevTools for traces/Lighthouse/heap, Playwright for E2E/visual). Acceptable.

### Missing Tool References
None outstanding after this cycle. The only stale/incorrect reference found — `requirements-analyst` → non-existent Linear MCP — was corrected. The only non-existent op names — `pm-agent` Tool Awareness citing Serena `write_memory`/etc. — were corrected to real Memory MCP ops.

## Upstream Comparison
- **New agents in repo**: 423 catalog agents across 28 categories scanned. Categories not in the roster are niche (blockchain-web3, game-development, finance, ffmpeg-clip-team, podcast-creator-team, obsidian-ops-team, ocr-extraction-team) or already covered by existing generalists. CHANGELOG additions (deployer/Vercel, Technical Debt Manager, Rootly Incident Responder) overlap existing agents (devops-engineer + `prepare-release`; refactoring-expert; debugger/root-cause-analyst+Sentry) or are vendor-specific SaaS. **None added** — consistent with the "don't bloat; quality over quantity" directive.
- **Updated agents upstream**: Diffed code-reviewer, backend-developer, multi-agent-coordinator, test-generator against catalog. Three (backend-developer, multi-agent-coordinator, test-generator) showed only divergent integration-list agent names that don't exist in this roster — the installed versions are *better adapted* and already carry richer Tool Awareness. **One genuine improvement pulled**: code-reviewer's **Automated Pre-Checks** section (grafted, environment-adapted). Note: the installed agents are heavily customized supersets (e.g., code-reviewer body 289 lines vs upstream 170), so wholesale upstream re-syncs would regress the local Tool Awareness/model-tier work — selective grafting is the correct strategy.

## Router Sync
- **AGENT_ROUTER.md**: Roster unchanged (30 agents = 29 specialists + coordinator), so the agents table, model tiers, and the coordinator's "29 specialist agents (excluding self)" count are all still accurate — verified, no table edits required.
- **One change made**: Added an **observability line** to the "MCP capabilities to surface in agent prompts" hint-list — `SigNoz/Sentry: observability — traces, metrics, logs, dashboards, alerts, error tracking, release health`, naming the six agents that should receive it — so the coordinator's prompt guidance stays consistent with the agent-level updates.

## Gaps & Recommendations
- **Candidate future additions (not acted on):** an **accessibility specialist** (currently folded into frontend-architect via WCAG/Playwright), an **api-graphql** specialist, and a **database/migration** specialist (currently split across backend-architect + data-engineer + PostgreSQL MCP). Only worth adding if the user starts doing sustained work in those areas — flagging, not recommending, to avoid roster bloat.
- **`sentry` MCP token placeholder:** `.mcp.json` has `github` with `GITHUB_PERSONAL_ACCESS_TOKEN: "<YOUR_TOKEN>"` (placeholder) — GitHub MCP calls referenced by ~10 agents will fail until a real token is set. Not an agent-file issue, but worth the user's attention since several agents now lean on GitHub MCP. (Sentry uses a hosted URL and needs no local token.)
- **pm-agent body pseudocode:** the 700-line pm-agent still carries an extensive Serena-style `write_memory`/`read_memory` pseudocode lifecycle (lines ~34–265). It's explicitly framed as illustrative by the line-22 note and the corrected Tool Awareness bullet, so it's not broken — but a future cycle could consider trimming/rewriting that lifecycle to the real Memory MCP entity-graph model for clarity. Left as-is this cycle to avoid a large rewrite of a working agent.
- **Next cycle:** re-verify SigNoz/Sentry remain connected (they were in the connecting-servers set this session); confirm whether a GitHub token has been configured; and re-scan the upstream CHANGELOG for any genuinely novel agent categories.

---

**Verification performed:** all 11 edited agent files + AGENT_ROUTER.md confirmed via read-back and a residual-pattern grep (0 stale "if Sentry configured" / Linear / "Not in the default MCP set" hits; 6 agents now carry SigNoz/Sentry; 0 fake memory-ops outside the intentionally-illustrative pm-agent pseudocode). The `/tmp/cct-review` clone was removed (cleanup confirmed). The guard-hooks memory was enriched with the exact gate mechanism. No agents added or removed; conservative on removals/additions, liberal on the 11 instruction updates, as directed.
