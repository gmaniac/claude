- **pm-agent body is heavy and partly non-English (Japanese PDCA).** This cycle's note corrects the Serena→Memory misalignment, but the ~700-line body remains the largest, least conventional agent (~3× any other). Consider a focused trim next cycle to bring it in line with the compact format.
- **Cosmetic MCP label cleanup**: `system-architect` and `learning-guide` call `mcp__grep__searchGitHub` "GitHub MCP." Relabel to "Grep MCP (searchGitHub)" next cycle to distinguish from the real `mcp__github__*` server.
- **Linear MCP is auth-gated**: `requirements-analyst` references it, but only `authenticate`/`complete_authentication` are exposed until you log in. If you use Linear, run the interactive auth once so the issue tools become available; otherwise the reference is harmless but inert.
- **Consider a `context-manager` agent**: many agent bodies open with "Query context manager for…" (a SuperClaude convention), but no such agent exists in the roster. It's currently a no-op phrase. Either add a lightweight context-manager (upstream `development-tools/context-manager.md` exists) or, cleaner, scrub the "query context manager" boilerplate next cycle since it references a non-existent agent.
- **Possible future additions if your work shifts**: `ml-engineer`/`mlops-engineer` (if you move from API consumption toward model training), or a dedicated `database` specialist (if schema/query work outgrows backend-architect + data-engineer). No action this cycle — neither is an unmet need today.
- **Next review cycle**: verify the new `prompt-engineer` is being selected for LLM/prompt work; trim pm-agent; scrub the "context manager" boilerplate; relabel the grep-MCP mentions.

---

## Verification
Post-change state confirmed on disk:
- **30 agent files** present (29 specialists + multi-agent-coordinator).
- **Zero** remaining `Sentry MCP` references across `~/.claude/agents/`.
- **Zero** remaining "28 specialist" references in router or coordinator.
- No `.backup`/`.bak` pollution in the agents directory (the Edit-backup hook did not fire on these tool calls).
- `/tmp/cct-review` removed.

All four tracked tasks completed. The roster is consistent, every MCP/skill reference now resolves to something actually available (or is explicitly marked as discover-if-configured), and the router reflects reality.
