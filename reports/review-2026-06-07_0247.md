- **Skill-invocation reality check**: I wired `Skill` into 14 agents on the principle that a referenced skill must be callable. If you find subagents can't resolve the `Skill` tool in practice, the fallback is to keep skill orchestration at the router/main-session level (the new "Skills to leverage" block already supports that). Worth confirming on the next real multi-agent run.
- **`pm-agent`**: consider adding `TaskCreate/TaskUpdate` awareness alongside its existing TodoWrite/Beads guidance, for in-session task tracking.
- **`filesystem` MCP** is configured with a placeholder path (`/path/to/allowed/files`) and `github` MCP with a `<YOUR_TOKEN>` placeholder in `~/.mcp.json` — neither is agent-facing, but if you intend to use them, those configs need real values. No agent change required.
- **Next cycle**: (1) decide the orchestrator nesting/Task-tool question above; (2) re-verify the 14 `Skill` wirings actually resolve at runtime; (3) roster remains well-scoped — continue resisting upstream's 421-agent sprawl and keep enhancing existing agents over adding new ones.

---

**Done.** 16 agent files updated, `AGENT_ROUTER.md` updated, 0 added, 0 removed, all 30 frontmatter blocks validated (name matches filename; model ∈ {opus, sonnet, haiku}; tools present), and `/tmp/cct-review` cleaned up.
